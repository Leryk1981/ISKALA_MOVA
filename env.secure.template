# ========================================================
# 🔐 ISKALA SECURE ENVIRONMENT CONFIGURATION TEMPLATE
# ========================================================
# 
# КРИТИЧЕСКИ ВАЖНО: 
# 1. Скопируйте этот файл в .env: cp env.secure.template .env
# 2. Замените ВСЕ placeholder значения на реальные секреты
# 3. НИКОГДА не коммитьте .env файл в git
# 4. Используйте сильные, уникальные пароли для production
#
# ⚠️  SECURITY WARNING: 
# Файл .env содержит КРИТИЧЕСКИЕ секреты - защитите его!
# ========================================================

# ================================
# 🌍 ENVIRONMENT CONFIGURATION
# ================================

# Environment Mode: development, staging, production, testing
ENVIRONMENT=development

# Debug Mode (ТОЛЬКО для development!)
DEBUG=true

# Logging Level: DEBUG, INFO, WARNING, ERROR, CRITICAL  
LOG_LEVEL=INFO

# ================================
# 🚨 КРИТИЧЕСКИЕ СЕКРЕТЫ (ОБЯЗАТЕЛЬНО ИЗМЕНИТЬ!)
# ================================

# OpenRouter API Key для LLM requests
# ⚠️  ЗАМЕНИТЕ на ваш реальный API key!
# Получить можно на: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-YOUR_REAL_OPENROUTER_API_KEY_HERE

# OpenRouter Model
OPENROUTER_MODEL=moonshotai/kimi-k2

# WebUI Secret Key для session security
# ⚠️  СГЕНЕРИРУЙТЕ сильный ключ: openssl rand -hex 32
WEBUI_SECRET_KEY=YOUR_STRONG_32_CHAR_SECRET_KEY_HERE_CHANGE_THIS_IMMEDIATELY

# ================================
# 🔑 API AUTHENTICATION
# ================================

# Enable API Key Authentication (рекомендуется true для production)
ENABLE_API_KEY_AUTH=true

# API Keys для Tool Server (разделенные запятыми)
# ⚠️  СГЕНЕРИРУЙТЕ уникальные ключи для каждого клиента
API_KEYS=your_api_key_1,your_api_key_2,your_api_key_3

# ================================
# 🗄️ DATABASE CREDENTIALS  
# ================================

# Neo4j Graph Database
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
# ⚠️  ЗАМЕНИТЕ на сильный пароль: openssl rand -base64 24
NEO4J_PASSWORD=YOUR_STRONG_NEO4J_PASSWORD_HERE_CHANGE_THIS
NEO4J_DATABASE=iskala-mova

# Redis Cache
REDIS_HOST=localhost
REDIS_PORT=6379
# ⚠️  ЗАМЕНИТЕ на сильный пароль для production: openssl rand -base64 24  
REDIS_PASSWORD=YOUR_STRONG_REDIS_PASSWORD_HERE_CHANGE_THIS
REDIS_DB=0

# ================================
# 🌐 WEB SERVER CONFIGURATION
# ================================

# Service Ports
ISKALA_PORT=8001
VAULT_PORT=8081
TRANSLATION_PORT=8082
RAG_PORT=8002
TOOL_SERVER_PORT=8003
GRAPH_SEARCH_PORT=8004

# CORS Configuration
# ⚠️  В production НЕ ИСПОЛЬЗУЙТЕ "*" - укажите конкретные домены!
# Development:
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000
# Production example:
# ALLOWED_ORIGINS=https://yourdomain.com,https://api.yourdomain.com

# Allow credentials в CORS (рекомендуется false)
CORS_ALLOW_CREDENTIALS=false

# ================================
# ⚡ PERFORMANCE & RATE LIMITING
# ================================

# Rate Limiting (requests per window)
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60

# Performance Settings
MAX_CONCURRENT_REQUESTS=100
REQUEST_TIMEOUT=30

# ================================
# 📊 MONITORING CONFIGURATION  
# ================================

# Enable Prometheus Metrics
ENABLE_METRICS=true
METRICS_PORT=9090

# ================================
# 🚨 SECURITY COMMANDS 
# ================================
#
# Генерация сильных паролей:
# openssl rand -base64 32          # Для паролей БД
# openssl rand -hex 32             # Для secret keys  
# openssl rand -base64 48          # Для API keys
#
# Проверка конфигурации:
# python -c "from iskala_basis.config.secure_config import load_config; print('✅ Config valid')"
#
# ======================================================== 